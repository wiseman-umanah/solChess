DATABASE_URL="postgresql://postgres:password@localhost:5432/solchess"
REDIS_URL="redis://localhost:6379"
JWT_SECRET="change-me-in-production"
PORT=3001
CORS_ORIGIN="http://localhost:5173"

# Solana / Anchor
# RPC endpoint — use localnet for dev, devnet/mainnet for production
SOLANA_RPC_URL="http://127.0.0.1:8899"
# Authority keypair — the wallet that signs settle_game, open_stakes, settle_stakes
# Format: JSON byte array  e.g. [1,2,3,...,64]  OR base58-encoded private key
AUTHORITY_KEYPAIR=""
# Treasury wallet public key (where platform fees land)
TREASURY_PUBKEY=""
